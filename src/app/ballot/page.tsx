import ComingSoon from "@/components/ui/ComingSoon";

export default function BallotPage() {
  return (
    <ComingSoon
      title="Your Ballot"
      description="Every race, matched to your values. From president to school board. Take the quiz first to build your civic profile — your ballot recommendations are built on top of it."
      accentColor="var(--color-red)"
    />
  );
}
