import ComingSoon from "@/components/ui/ComingSoon";

export default function BeyondBallotPage() {
  return (
    <ComingSoon
      title="Beyond Your Ballot"
      description="The races that aren't on your ballot still shape your country. Find candidates beyond your own district worth supporting — the ones whose presence in Congress would make a real difference — matched to your civic values."
      accentColor="var(--color-rose)"
    />
  );
}
